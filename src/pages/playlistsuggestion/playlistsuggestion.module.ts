import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PlaylistsuggestionPage } from './playlistsuggestion';

@NgModule({
  declarations: [
    PlaylistsuggestionPage,
  ],
  imports: [
    IonicPageModule.forChild(PlaylistsuggestionPage),
  ],
})
export class PlaylistsuggestionPageModule {}
